# gsmexteriors.com

GSM Exterior Services Inc. — pressure washing, soft washing & window cleaning (Greater Houston, TX).
Single-page bilingual (EN/ES) site served with GitHub Pages.

## Edit checklist
- **Phone**: search `(832) 853-1964` and `+18328531964` in `index.html` to swap the number.
- **Lead form**: powered by Web3Forms — `WEB3FORMS_KEY` variable near the top of the `<script>` block. Submissions email the inbox registered with that key; manage at web3forms.com.
- **Form fields**: name, phone, email (optional), service, property type, property address (required — used to quote over the phone), notes.
- **Reviews**: the three testimonials are SAMPLE text — replace with real customer reviews.
- **Brand assets**: `img/brand/` — light/dark wordmark SVG+PNG, icon, and navy social version.
- **Custom domain**: add a `CNAME` file containing `gsmexteriors.com` and point DNS (A records to GitHub Pages IPs, or CNAME `www` to `adminstarlingagent.github.io`).
