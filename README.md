# gsmexteriors.com

GSM Exteriors — pressure washing, soft washing & window cleaning (Greater Houston, TX).
Single-page bilingual (EN/ES) site served with GitHub Pages.

## Edit checklist
- **Phone**: search `832) 808-6483` and `+18328086483` in `index.html` to swap the number.
- **Lead form email**: `FORM_EMAIL` variable near the top of the `<script>` block (uses formsubmit.co — first submission sends a one-time activation email to that inbox).
- **Reviews**: the three testimonials are SAMPLE text — replace with real customer reviews.
- **Before/after**: the slider uses placeholder graphics — swap the `.ba-before` / `.ba-after` backgrounds for real photos.
- **Custom domain**: add a `CNAME` file containing `gsmexteriors.com` and point DNS (A records to GitHub Pages IPs, or CNAME `www` to `adminstarlingagent.github.io`).
